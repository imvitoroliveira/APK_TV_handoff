import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clientestoptv.meustream.manager',
  appName: 'Meu Stream Gestor',
  webDir: 'dist'
};

export default config;
